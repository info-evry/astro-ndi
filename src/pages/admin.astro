---
/**
 * NDI Admin Dashboard
 *
 * Thin wrapper around AdminPage.astro so that deploying this page under
 * a secret path (e.g. src/pages/some/secret/path.astro) only requires
 * copying this file — it only imports via the `@/` alias, which resolves
 * from src/ regardless of this file's own location.
 */
import AdminPage from '@/components/admin/AdminPage.astro';
---

<AdminPage />
